{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "HUSKY=0 npm install --ignore-scripts && npx prisma generate && cd apps/api && npm install --ignore-scripts && npm run build",
    "startCommand": "node apps/api/dist/main"
  }
}
